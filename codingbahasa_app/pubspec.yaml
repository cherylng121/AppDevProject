name: codingbahasa_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  file_picker: ^6.0.0
  open_file: ^3.3.2
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  
  # Firebase packages
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.14.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  
  # State management
  provider: ^6.1.2

  # BLoC state management for AI Chatbot
  bloc: ^8.1.0
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5

  # File handling
  file_picker: ^5.5.0
  open_file: ^3.3.1

  # PDF Viewer (new stable version compatible with latest Flutter)
  syncfusion_flutter_pdfviewer: 31.2.4

  # YouTube video player
  youtube_player_flutter: ^9.1.3

  # WebView support (latest stable version)
  webview_flutter: ^4.8.0

  # URL launcher for opening links
  url_launcher: ^6.3.0

  # Internationalization / Date formatting
  intl: ^0.19.0

  # Modern in-app webview plugin
  flutter_inappwebview: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true